// describe("Testing", function() {
//   it("is going so smoothly", function() {
//     let testing = document.getElementById('title');
//     expect(testing.innerHTML.includes('Prepr')).toEqual(true);
//   });
//   it("is not going so smoothly", function() {
//     expect(2).toEqual(1);
//   });
// });

describe("Testing", function() {
  it("is going so smoothly", function() {
    expect(1).toEqual(1);
  });
  it("is not going so smoothly", function() {
    expect(2).toEqual(1);
  });
});
